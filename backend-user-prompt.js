//---------------------------(04/04/22)------------------------------
// const prompt  =  require ("prompt-sync") ({signit: true});

// let name = prompt("Do It One More Time: ");
// console.log(`Hello ${name}`);

//Ex.)
// const prompt = require("prompt-sync") ({sigint: true});
// let input1 = prompt("This is test 1 of user input: ");
// console.log(`Test of ${input1} was successful`);

//Ex.)
const prompt = require("prompt-sync") ({sigint: true});
let input2 = prompt("Type Your Name Again: ");
console.log(`This is the 2nd Test of ${input2}`);

//Ex.3)
// const prompt = require ("prompt-sync") ({sigint: true});

// const prompt = require("prompt-sync") ({sigint: true});

// const prompt = require("prompt-sync") ({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync") ({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync") ({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

// const prompt = require("prompt-sync")({sigint: true});

//-------------------------------------------------------------
